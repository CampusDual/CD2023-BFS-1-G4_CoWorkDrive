<?xml version="1.0" encoding="UTF-8"?>
<JdbcEntitySetup
        xmlns="http://www.ontimize.com/schema/jdbc"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.ontimize.com/schema/jdbc http://www.ontimize.com/schema/jdbc/ontimize-jdbc-dao.xsd"
        catalog="" schema="${mainschema}" table="trip"
        datasource="mainDataSource" sqlhandler="dbSQLStatementHandler">
    <DeleteKeys>
        <Column>id_trip</Column>
    </DeleteKeys>
    <UpdateKeys>
        <Column>id_trip</Column>
    </UpdateKeys>
    <GeneratedKey>id_trip</GeneratedKey>
    <Queries>
        <Query id="all_trips">
            <Sentence>
                <![CDATA[
                     SELECT
                      #COLUMNS#
                    FROM trip
                    #WHERE#
                        AND date >= cast(now() as date)
                   ]]>
            </Sentence>
        </Query>
        <Query id="get_user">
            <Sentence>
                <![CDATA[
                     SELECT
                      id_user
                    FROM trip
                    #WHERE#
                   ]]>
            </Sentence>
        </Query>
        <Query id="booking_info">
            <AmbiguousColumns>
                <AmbiguousColumn name="id_trip" prefix="t" />
            </AmbiguousColumns>
            <ValidColumns>
                <Column>origin</Column>
                <Column>destination</Column>
                <Column>date</Column>
                <Column>time</Column>
                <Column>name</Column>
                <Column>surname1</Column>
                <Column>surname2</Column>
                <Column>free_seats</Column>
                <Column>id_trip</Column>
            </ValidColumns>
            <Sentence>
                <![CDATA[
                    SELECT t.origin, t.destination, t.date, t.time, u.name, u.surname1 , u.surname2, (c.seats - count(b.*)) as free_seats, t.id_trip
                    FROM trip t JOIN tuser u ON t.id_user = u.user_
                    JOIN car c ON u.user_ = c.id_user
                    LEFT JOIN booking b ON b.id_trip = t.id_trip
                    #WHERE#
                    GROUP BY t.origin, t.destination, t.date, t.time, u.name, u.surname1 , u.surname2, c.seats, t.id_trip
                   ]]>
            </Sentence>
        </Query>
        <Query id="trip_detail">
            <AmbiguousColumns>
                <AmbiguousColumn name="id_trip" prefix="t" />
                <AmbiguousColumn name="id_user" prefix="c" />
            </AmbiguousColumns>
            <ValidColumns>
                <Column>origin</Column>
                <Column>destination</Column>
                <Column>date</Column>
                <Column>time</Column>
                <Column>number_cars</Column>
                <Column>id_trip</Column>
                <Column>id_user</Column>
            </ValidColumns>
            <Sentence>
                <![CDATA[
                    SELECT count(c.*) as number_cars, t.origin, t.destination, t.date, t.time, t.id_trip, c.id_user
                    FROM trip t
                    RIGHT JOIN car c ON t.id_user = c.id_user
                    #WHERE#
                    GROUP BY t.id_trip, c.id_user
                   ]]>
            </Sentence>
        </Query>
    </Queries>
</JdbcEntitySetup>